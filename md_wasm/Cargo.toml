[package]
name = "md_wasm"
version = "0.1.0"
edition = "2024"

[dependencies]
#pulldown-cmark="0.13"
pulldown-cmark= { version = "0.10.0", default-features = false, features = ["html"] }
wasm-bindgen = "0.2"

[lib]
crate-type = ["cdylib"]