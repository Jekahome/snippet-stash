


<pre><code class="language-rust">
use std::cell::Cell;
fn main(){
   let v:Vec<i32> = vec![1];
   let mut c = Cell::new(v);
 
   let value:&mut Vec<i32> = c.get_mut();
   value.push(2);
   // let value:&Vec<i32> = c.get();// doesn't satisfy `Vec<i32>: Copy`
    println!("{:?}",value);// [1,2]
}
</code></pre>
