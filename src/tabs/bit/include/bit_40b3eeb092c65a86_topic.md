

Представление байтов

* **`u8`** — базовый тип для одного байта.
* **`[u8]`** — срез байтов.
* **`Vec<u8>`** — динамический буфер байтов.
* **`&[u8]`** часто используется для передачи данных в API (например, чтение файлов, сокеты).
