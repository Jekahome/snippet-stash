

std::panic::**set_hook**

Хуки паник работают только в том потоке, в котором они были установлены.
Использование хуков не предотвращает завершение программы при панике, если только вы не используете std::panic::**catch_unwind** для перехвата паники.
