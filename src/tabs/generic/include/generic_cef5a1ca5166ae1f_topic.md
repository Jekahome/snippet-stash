

`fn type_id(&self) -> TypeId`

Трейт `Any` реализуется автоматически для всех типов, не содержащих не - `'static` ссылки. 
Когда трейт находится в области видимости, вы можете вызвать `type_id` метод в значении, чтобы узнать, какой у них тип:
