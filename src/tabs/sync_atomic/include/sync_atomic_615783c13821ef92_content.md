

Эта библиотека определяет общий тип атомарной оболочки `Atomic<T>` для всех `T: NoUninit` типов.
Привязка NoUninit взята из крейта bytemuck и указывает, что тип не имеет внутренних байтов заполнения. 
Вам нужно будет получить или реализовать эту черту для всех типов, используемых с `Atomic<T>`
