* Чтение с консоли — stdin()
```
use std::io::Write;
let mut line = String::new();
println!("Enter your name :");
let b1 = std::io::stdin().read_line(&mut line).expect("Не удалось ввести данные");
println!("Hello , {}", line);
println!("no of bytes read , {}", b1);
```
