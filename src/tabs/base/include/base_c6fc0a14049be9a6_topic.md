

Если `lifetime` не указаны явно, то для каждого `'_` в параметрах
создаётся свой уникальный `lifetime`, а возвращаемое значение
получает `lifetime` `self` или уникальное `lifetime` параметра. 
Lifetime Elision компилятор не смотрит на тело функции.
