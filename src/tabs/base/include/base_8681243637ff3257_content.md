


<pre><code class="language-rust">
fn foo_gen_type<T:Debug+Copy,R:Deref<Target = T>>(arg:R){
    //let v = &*arg;
    // или
    let v = *Deref::deref(&arg);
    println!("{:?}",v);
}
// или ----------------------------------------------
fn foo_impl_type<R:Deref<Target = i32>>(arg:R){
    //let v = &*arg;
    // или
    let v = *Deref::deref(&arg);
    println!("{:?}",v);
}
// или ----------------------------------------------
struct Foo<T:Debug+Copy,R:Deref<Target = T>+Copy+Debug>{
    data:R
}
impl <T:Debug+Copy,R:Deref<Target = T>+Copy+Debug>Foo<T,R>{
    fn new(data:R)->Self{
        Self{data}
    }
    fn show_deref(&self){
        println!("{:?}",self.data);
    }
}
fn main() {
    foo_gen_type::<i32,&i32>(&5);     
    foo_impl_type::<&i32>(&5);  
    let  f = Foo::<i32,&i32>::new(&5);
    f.show_deref();
    let data:i32 = *f.data;
}
</code></pre>
