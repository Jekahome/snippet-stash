

Можно вернуть переданный аргумент обратно 
<pre><code class="language-rust">
fn foo2(v:Vec<i32>) -> Vec<i32> { v } 

но лучьше передавать в ф-цию данные по ссылке это наз. Заимствование и данные не удалятся
fn foo2(v:&mut Vec<i32>) {  } 
</code></pre>
