

Предпочтительней. Постоянные живут в течение всего времени работы программы. 
Вычисляются только во время компиляции.
А именно, у них вообще нет определённого адреса в памяти !!!
 
тип постоянной надо указывать всегда.

`const` может быть:
 - использование констант из внешних крейтов
 - ссылка должна иметь время жизни `'static`
